// Import the rand crate, and fill the flip_coin method to make it return a Coin enum.
// https://docs.rs/rand/latest/rand/

enum Coin {
    Heads,
    Tails
}

fn flip_coin() -> Coin {
}

fn main() {
    // Flip a coin, then thanks to the "match" keyword display "Head" or "Tail".
    // use println!() do display
}

